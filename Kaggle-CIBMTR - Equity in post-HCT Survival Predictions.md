# Kaggle-CIBMTR - Equity in post-HCT Survival Predictions

**项目背景**：提升接受异体造血细胞移植（HCT）患者的移植存活率预测

**评价指标**：分层一致性指数（C-index）

一致性指数（C-index）是生存分析中评估模型预测效果的核心指标，取值范围在 0.5（随机猜测）到 1.0（完美预测）之间，值越高说明预测效果越好。

按 `race_group`（种族分组）分别计算每组的**一致性指数（C-index）**，返回「所有种族分组 C-index 的均值 - 这些 C-index 的标准差」作为最终评分

**数据介绍**：

数据集包含59个与造血干细胞移植（HSCT）相关的变量，涵盖受体和捐献者的多种人口统计和医疗特征，如年龄、性别、族裔、疾病状况及治疗细节。主要关注的结果是无事件存活，由变量efs表示，而到无事件生存的时间则由变量efs_time表示。这两个变量共同编码了用于审查事件时间分析的目标。

**项目重点**：

1.预测患者发生事件的概率（efs）

2.预测发生事件的患者的排名（efs_time）

## **自己方案**：

### **深度学习方案：**

通过多目标回归拟合cox目标以及基于数据中的 `efs`（二元标识，取值为 0 或 1）和 `efs_time`（时间类数值）字段，通过**数值偏移、排序、缩放、对数变换、中心化、符号反转**等一系列操作，构建一个自定义的目标变量 y，拉大 0/1 类样本的差距，用CNN和DNN网络进行预测

### **机器学习方案**：

分别用两套特征，对xgb进行了一个分类预测efs，然后用xgb对cox，kap，custom目标进行回归，lgb和cat对kap目标进行拟合回归。对于efs=1的患者重点关注，加一定数值

对上述模型赋予不同权重进行加权融合

![image-20260124103612365](F:\桌面\Kaggle-CIBMTR - Equity in post-HCT Survival Predictions.assets\image-20260124103612365.png)

## 前排方案：

训练 CatBoost 分类模型，预测 “样本是否（efs==0）”：

1.分层交叉验证：按`efs+race_group`分层，避免某种族 / 事件类型在验证集占比失衡；

2.模型参数：适配二分类任务（Logloss 损失），优化 AUC 指标，设置子采样 / 列采样防止过拟合；

3.OOF 预测：保存各折验证集的预测结果，用于后续模型融合；<br>



训练 LightGBM 回归模型，预测生存时间的归一化排名：

1.回归目标构建：按`efs`分组对`efs_time`做归一化排名（消除不同分组的量纲差异）；

2.样本权重：侧重事件发生样本（权重 0.6），因为这类样本的生存时间更有参考价值；

3.特征新增：将`efs`作为特征输入；

搜索回归和分类的非线性加权融合比例，得到最优分数

